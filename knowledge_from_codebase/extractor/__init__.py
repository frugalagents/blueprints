"""
extractor — Pipeline-facing wrappers for rule extraction and flow mapping.

These modules bridge ``main.py`` to the classifier package's
:class:`~classifier.rule_extractor.RuleExtractor` and
:class:`~classifier.flow_mapper.FlowMapper`.
"""
